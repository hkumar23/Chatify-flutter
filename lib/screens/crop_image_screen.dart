import 'package:flutter/material.dart';

class CropImageScreen extends StatelessWidget {
  const CropImageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Crop Image Screen"),
    );
  }
}
